<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Lista de musicas Cadastrados</title>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-5">
        <a class="navbar-brand" href="#">Sistema Musical</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="/">Home
                        <span class="sr-only">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/musicas">Lista de Músicas</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/musicas/form">Cadastro</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container">
        <h2 class="text-center mb-3">Listagem das Musicas</h2>

        <table class="table">
            <thead class="table-primary">
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Nome</th>
                    <th scope="col">Duração</th>
                    <th scope="col">Artistas</th>
                    <th class="scope center">Ações</th>
                </tr>
            </thead>
            <tbody>
                <% for(var i=0; i < lista.length; i++) { %>
                <tr>
                    <th scope="row">
                        <%=i+1%>
                    </th>
                    <td>
                        <%= lista[i].nome %>
                    </td>
                    <td>
                        <%= lista[i].duracao %>
                    </td>
                    <td>
                        <%= lista[i].artistas %>
                    </td>
                    <td>
                        <a href="/musica/edita/<%= lista[i].id %>" class="btn btn-outline-primary btn-sm">Editar</a>

                        <form method="post" action="/musicas/remove/<%= lista[i].id %>" class="float-left">
                            <button type="submit" class="btn btn-outline-danger btn-sm"
                                onClick="return confirm('Confirmar a exclusão do musica<%= lista[i].nome %>?')">Excluir</button>

                        </form>

                    </td>
                </tr>
                <% } %>
            </tbody>

        </table>
    </div>
</body>

</html>